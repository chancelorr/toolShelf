Some important commands for pushing updates and all that

To build the package, run from package directory:

rmt dist/*
python setup.py sdist bdist_wheel

To upload the package to PyPI, run from package directory:

twine upload dist/*
